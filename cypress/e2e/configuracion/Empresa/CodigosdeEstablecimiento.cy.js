require("cypress-xpath");
require("cypress-plugin-tab");
describe("Configuracion Empresa - Codigos de Establecimiento", () => {
  Cypress.on("uncaught:exception", (err, runnable) => {
    return false;
  });
  it("Prueba 1", () => {});
});
